databases:
  - name: crossword-db
    plan: free
    databaseName: crossword
    user: crossword

services:
  - type: web
    plan: free
    name: crossword
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: crossword-db
          property: connectionString
