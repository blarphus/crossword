services:
  - type: web
    plan: free
    name: crossword
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: polycast-sequel-db
          property: connectionString
      - key: TZ
        value: America/New_York
